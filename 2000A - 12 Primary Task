#include<bits/stdc++.h>
using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	int t;
	cin >> t;
	while(t--){
		string n;
		cin >> n;
		if (n[0] == '1' && n.length()>=3 && n[1] == '0' && n[2] != '0' && stoi(n)%100 >= 2){
			cout << "YES\n";
		}
		else{
			cout << "NO\n";
		}
	}

	return 0;
} 
