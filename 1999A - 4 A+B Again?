#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;
    for (int i = 0;i<t;i++){
        int num;
        int sum = 0;
        cin >> num;
        sum = sum + (num / 10);
        num = num % 10;
        sum = sum + num;
        cout << sum << "\n";
    }
}
